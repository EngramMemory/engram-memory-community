qdrant-client>=1.7.0
requests>=2.28.0
numpy>=1.21.0
click>=8.0.0
pyyaml>=6.0
httpx>=0.24.0
kuzu>=0.4.0
scikit-learn>=1.3.0

# /graph slash command — graphify is vendored under vendor/graphify/
# (MIT, github.com/safishamsi/graphify). The only runtime dep the engram
# /graph path actually needs is networkx; graspologic is optional and
# enables Leiden community detection (falls back to Louvain if absent).
networkx>=3.0
# graspologic>=3.4  # optional — better community detection, heavy install
